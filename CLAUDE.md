# CLAUDE.md — مطعم و مطبخ السعدي

## هدف المشروع
موقع مطعم عربي (RTL) مع نظام طلب أونلاين ولوحة تحكم بسيطة، لمطعم و مطبخ السعدي
في درعا - الصنمين. مشروع تدريبي قد يُعرض على صاحب المطعم الحقيقي.

## معلومات المطعم المؤكدة (لم تُخترع، مصدرها صاحب المطعم مباشرة)
- الاسم: مطعم و مطبخ السعدي
- العنوان: درعا - الصنمين (رابط خرائط: https://maps.app.goo.gl/9QJpwKundpcr2aNT6)
- مناطق التوصيل: قرى وبلدات ريف درعا
- الهاتف: +963944886083
- واتساب: +963982673401
- ساعات العمل: من 8 صباحاً حتى 4 فجراً
- إنستغرام: instagram.com/muhammad._300
- فيسبوك: facebook.com/share/1F5uGs1yry
- طرق الدفع: الدفع عند الاستلام (فعّال) / محفظة محلية (غير محددة بعد — معطّلة في الواجهة)

## التقنيات
Next.js 16 (App Router) · TypeScript · Tailwind CSS · Zustand · Supabase
(Postgres + Auth + Storage + RLS). لا يوجد باكند أو قاعدة بيانات أخرى.

## قرارات قاعدة البيانات
- 5 جداول: categories, menu_items, orders, order_items, profiles
- order_items تحفظ نسخة (snapshot) من اسم الصنف وسعره وقت الطلب، حتى تبقى
  الطلبات القديمة صحيحة تاريخياً لو تغيّر المنيو لاحقاً
- profiles هو مصدر صلاحية الإدارة: أي مستخدم auth بدون صف في profiles
  لا يُعامل كأدمن، حتى لو سجّل حساباً بنفسه (sign up لا يمنح صلاحيات تلقائياً)
- RLS مفعّل على كل الجداول ومُتحقق منه عبر Supabase Advisor (راجع testing.md)

## الأصول الحقيقية المستلمة
- الشعار: صورة نظيفة الخلفية (checker) بصيغة JPEG فعلية (بدون شفافية حقيقية)
  → استُخدم كما هو، لم يُعدَّل أو تُنشأ نسخة PNG شفافة لأن ذلك يتطلب أداة
  إزالة خلفية غير متوفرة، ومحاولة يدوية كانت ستُتلف الأجزاء البيضاء من الشعار
- صورة الواجهة: صورة المطعم ليلاً (لافتة مضاءة) — مستخدمة في قسم Hero
- صورتا طعام حقيقيتان: منسف، شاورما عربي — مؤكَّدتان من صاحب المطعم ومربوطتان
  فعلياً في قاعدة البيانات كملفات ثابتة داخل مجلد public (وليس عبر Storage،
  لأنها تُشحن مع التطبيق نفسه ولا تحتاج رفعاً لاحقاً)

## الأصول المولّدة
لا توجد صور مولّدة بالذكاء الاصطناعي في هذا المشروع. لا توجد أداة توليد صور
متاحة في بيئة التنفيذ. بدلاً من ذلك: الأصناف الـ13 المتبقية بدون صورة حقيقية
لها image_url = NULL، وتُعرض بحالة "الصورة قيد الإضافة" احترافية (انظر
missing-image-state.tsx) بدل صورة وهمية أو أيقونة مكسورة.

## قرارات أمنية
- القراءة العامة: التصنيفات وجميع الأصناف (available فقط للزوار، admin يرى الكل)
- الكتابة العامة: إنشاء طلب وأصناف الطلب فقط (INSERT)، لا قراءة ولا تعديل
- الأدمن فقط: إدارة التصنيفات/الأصناف، قراءة/تحديث الطلبات
- صفحة تأكيد الطلب لا تُعيد قراءة الطلب من القاعدة (RLS يمنع ذلك للزوار
  عمداً، لمنع أي شخص من قراءة طلب غيره بتخمين رقم الطلب) — بدلاً من ذلك تُقرأ
  البيانات من حالة العميل المحلية (last-order-store) التي أُنشئت لحظة الطلب
- لا يوجد service role key في أي كود عميل أو خادم — فقط anon/publishable key

## الوضع الحالي
انظر journal/progress.md

## قيود المشروع
- بيئة التنفيذ الحالية لا تملك اتصال إنترنت فعلي ولا أداة توليد صور، لذلك:
  لم يُشغَّل npm install / next build فعلياً هنا — يجب تشغيله محلياً للتحقق
- تفعيل حساب الأدمن الأول يتطلب إدخال يدوي (SQL) لربط user_id بجدول profiles
  (خطوة تُنفَّذ مرة واحدة فقط) — التفاصيل في README.md
