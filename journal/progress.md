# progress.md

## المهام المكتملة
- [x] التحقق من اتصال Supabase MCP (list_organizations, list_projects — أدلة حقيقية)
- [x] تصميم وإنشاء 5 جداول قاعدة بيانات + فهارس
- [x] تفعيل RLS وكتابة كل السياسات (public/admin) + دالة is_admin()
- [x] إصلاح تحذير أمني حقيقي من Supabase Advisor (search_path على is_admin)
- [x] إنشاء Storage bucket عام (menu-images) مع سياسات RLS للكتابة (أدمن فقط)
- [x] إدخال 3 تصنيفات و19 صنفاً حقيقياً (بلا أسعار/أوصاف مُخترعة)
- [x] ربط الصورتين الحقيقيتين (منسف، شاورما عربي) كملفات ثابتة في التطبيق
- [x] بناء الموقع العام: Navbar, Hero, About, Menu (تبويبات + بطاقات), Contact, Footer
- [x] حالة "لا توجد صورة" احترافية بدل صور وهمية أو أيقونات مكسورة
- [x] سلة تسوق Zustand (بدون إجمالي مضلل عند وجود أسعار غير محددة)
- [x] صفحة Checkout + Server Action لإنشاء الطلب فعلياً في Supabase
- [x] صفحة تأكيد الطلب (تعرض رقم الطلب، الملخص، طريقة الدفع، الحالة)
- [x] Middleware لحماية مسارات /admin
- [x] تسجيل دخول/خروج/تسجيل حساب (Supabase Auth)
- [x] لوحة تحكم: الطلبات (عرض + تحديث الحالة)، التصنيفات (CRUD)، الأصناف (CRUD)
- [x] رفع/استبدال/إزالة صورة الصنف من لوحة التحكم (Supabase Storage)
- [x] الوضع الفاتح/الداكن (Zustand + class على html)

## المهمة الحالية
تسليم الحزمة الكاملة للمستخدم مع تعليمات تشغيل حقيقية وصادقة.

- [x] إصلاح حلقة إعادة توجيه لا نهائية كانت تمنع ظهور لوحة التحكم بالكامل
      (نقل الصفحات المحمية إلى مجموعة مسارات `(protected)`، فصل /admin/login)

## المهام المتبقية (تتطلب بيئة حقيقية أو معلومات إضافية من المالك)
- [ ] تشغيل npm install فعلياً والتحقق من نجاح npm run build (لم يحدث هنا لعدم
      توفر إنترنت في بيئة التنفيذ الحالية)
- [ ] اختبار فعلي على متصفح حقيقي عبر جميع أحجام الشاشات المذكورة في المواصفة
- [ ] ربط أول حساب أدمن حقيقي بجدول profiles (خطوة يدوية لمرة واحدة)
- [ ] تحديد اسم/تفاصيل المحفظة المحلية عند توفرها من صاحب المطعم
- [ ] إضافة الأسعار الحقيقية عند تأكيدها من صاحب المطعم (عبر لوحة التحكم)
- [ ] رفع صور حقيقية إضافية للأصناف الـ13 المتبقية عند توفرها (عبر لوحة التحكم)
